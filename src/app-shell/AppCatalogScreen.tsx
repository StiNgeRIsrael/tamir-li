import { NativeHomeView } from "@/components/NativeHomeView";

/** Tools catalog tab — launcher grid without home greeting/upsell strip. */
export function AppCatalogScreen() {
  return <NativeHomeView variant="catalog" />;
}
