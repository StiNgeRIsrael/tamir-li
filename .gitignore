# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets (never commit)
.env
backend/.env
backend/.google-play-sa.json

# Local Android SDK (cap:sync / android:bundle)
.android-sdk/

# Cursor local (commit shared MCP config only)
.cursor/*
!.cursor/mcp.json

# GSC manual indexing progress (local per machine)
gsc-indexing-progress.txt
gsc-request-indexed.txt
gsc-daily-batch.txt
.gsc-playwright-profile/
gsc-debug-last.png
gsc-run-output.txt

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?